//functions loops logical code of a program together
function getAverage(a, b) {

    var average = (a + b) / 2;

    //console.log(average);

    return average;
}
let myResult = getAverage();
console.log('The average is '+ myResult );